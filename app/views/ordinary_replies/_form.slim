= simple_form_for @ordinary_reply do |f|
  = f.input :keyword

  - %w(text asset).each do |type|
    .btn.btn-success(data-toggle = "modal" data-target = "##{type}-modal")
      = type.capitalize
    | &nbsp
    = render "modal_partial", type: type, f: f
  p
  p = f.button :submit, value: "submit"
